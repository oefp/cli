# OEFP CLI

Command-line tools for OEFP.

## Development

Run the CLI locally:

```sh
go run ./cmd/oefp
```

Build the `oefp` executable:

```sh
go build ./cmd/oefp
```

Install it into your Go binary directory:

```sh
go install ./cmd/oefp
```